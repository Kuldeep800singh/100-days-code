#include<iostream>
#include<SFML/Graphics.hpp>

int main(){
	sf::RenderWindow window(sf::VideoMode(800,600),
							"DSA testing",
							sf::Style::Default);
	sf::Event events;
	while(window.isOpen()){
		
	}
							
}
